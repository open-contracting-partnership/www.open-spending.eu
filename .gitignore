/vendor/
/node_modules/
composer.lock
.DS_Store

# Local dev server (see Makefile / dev/) — production backups are sensitive,
# never commit them; lockfiles here are regenerated by `make build`.
/*.tar
/*.sql
/yarn.lock
/package-lock.json
/dist/mix-manifest.json
